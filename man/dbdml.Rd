% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbdml.R
\name{dbdml}
\alias{dbdml}
\title{Partially linear estimation on database backends}
\usage{
dbdml(
  fml,
  treat = NULL,
  method = c("auto", "strata", "plm"),
  conn = NULL,
  table = NULL,
  data = NULL,
  path = NULL,
  vcov = c("hc1", "iid", "none", "bootstrap"),
  cluster = NULL,
  folds = 5,
  ridge = 1,
  n_bootstraps = 0,
  seed = 42,
  sql_only = FALSE,
  data_only = FALSE,
  drop_missings = TRUE,
  verbose = getOption("dbreg.verbose", FALSE),
  ...
)
}
\arguments{
\item{fml}{A formula with one outcome on the left-hand side. All variables
on the right-hand side are candidate treatments or controls.}

\item{treat}{Character vector naming treatment variable(s). If \code{NULL}, the
first variable on the first RHS part is used.}

\item{method}{Character string choosing the estimator:
\code{"auto"} (default), \code{"strata"}, or \code{"plm"}.}

\item{conn}{Database connection. If \code{NULL}, an ephemeral DuckDB connection is
created and closed on exit.}

\item{table, data, path}{Mutually exclusive data source arguments with
precedence \verb{table > data > path}.}

\item{vcov}{Character string or formula. Character options are \code{"iid"},
\code{"hc1"}, \code{"none"}, and \code{"bootstrap"}.}

\item{cluster}{Optional cluster variable (formula or character). If supplied,
clustered inference is used in the PLM second stage.}

\item{folds}{Number of folds for \code{method = "plm"}. Default is 5.}

\item{ridge}{Ridge penalty used in PLM nuisance fits. Either a single
non-negative scalar or a list like \code{list(y = 1, d = 1)}.}

\item{n_bootstraps}{Number of group bootstrap replications for
\code{vcov = "bootstrap"} in \code{method = "strata"}.}

\item{seed}{Integer random seed used by bootstrap.}

\item{sql_only, data_only}{Logical. Supported only for \code{method = "strata"}.}

\item{drop_missings}{Logical indicating whether rows with missing values in
required variables should be dropped.}

\item{verbose}{Logical indicating whether progress messages are printed.}

\item{...}{Additional unused arguments.}
}
\value{
A list of class \code{"dbdml"}.
}
\description{
Unified interface for partially linear estimation with two internal engines:
\itemize{
\item \code{method = "strata"}: A DuckDML-style leave-one-out (LOO) partialling-out
estimator for discrete controls, computed from compressed group statistics.
\item \code{method = "plm"}: A cross-fitted partially linear model (PLM) estimator.
Nuisance fits use \code{dbreg} moments by default and automatically fall back
to \code{dbreg} compression when controls include categorical variables.
}

With \code{method = "auto"} (default), \code{dbdml} chooses the engine based on the
control variables and inference request.
}
\examples{
set.seed(1)
n = 400
g = sample(1:20, n, TRUE)
d = rnorm(n)
y = 1 + 2 * d + rnorm(20)[g] + rnorm(n)
df = data.frame(y = y, d = d, g = factor(g))

# auto -> strata engine (discrete control g)
dbdml(y ~ d + g, data = df, treat = "d")

# explicit PLM engine
x = rnorm(n)
d = 0.5 * x + rnorm(n)
y = 1.5 * d + 0.8 * x + rnorm(n)
df2 = data.frame(y = y, d = d, x = x)
dbdml(y ~ d + x, data = df2, method = "plm", treat = "d")
}

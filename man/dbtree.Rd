% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbtree.R
\name{dbtree}
\alias{dbtree}
\title{Run a regression tree on a database backend}
\usage{
dbtree(
  fml,
  conn = NULL,
  table = NULL,
  data = NULL,
  path = NULL,
  weights = NULL,
  bin_scope = c("global", "node"),
  n_bins = 128,
  bin_method = c("quantile", "width"),
  bin_sample_frac = NULL,
  max_depth = 5,
  min_split = 20,
  min_leaf = 5,
  min_gain = 0,
  max_cat = 50,
  mtry = NULL,
  seed = NULL,
  drop_missings = TRUE,
  verbose = getOption("dbreg.verbose", FALSE),
  ...
)
}
\arguments{
\item{fml}{A \code{\link[stats]{formula}} representing the relation to be estimated.
Interactions are not supported. Fixed effects after \code{|} are not supported.}

\item{conn}{Database connection, e.g. created with
\code{\link[DBI]{dbConnect}}. Can be either persistent (disk-backed) or
ephemeral (in-memory). If no connection is provided, then an ephemeral
\code{\link[duckdb]{duckdb}} connection will be created automatically and
closed before the function exits. Note that a persistent (disk-backed)
database connection is required for larger-than-RAM datasets in order to take
advantage of out-of-core functionality like streaming (where supported).}

\item{table, data, path}{Mutually exclusive arguments for specifying the data
table (object) to be queried. In order of precedence:
\itemize{
\item \code{table}: Character string giving the name of the data table in an
existing (open) database connection.
\item \code{data}: R dataframe that can be copied over to \code{conn} as a temporary
table for querying via the DuckDB query engine. Ignored if \code{table} is
provided.
\item \code{path}: Character string giving a path to the data file(s) on disk, which
will be read into \code{conn}. Internally, this string is passed to the \code{FROM}
query statement, so could (should) include file globbing for
Hive-partitioned datasets, e.g. \code{"mydata/**/.*parquet"}. For more precision,
however, it is recommended to pass the desired database reader function as
part of this string, e.g. \code{"read_parquet('mydata/**/*.parquet')"} for DuckDB;
note the use of single quotes.
Ignored if either \code{table} or \code{data} is provided.
}}

\item{weights}{Character string specifying the column name to use as weights,
or \code{NULL} (default) for unweighted trees. Weights must be non-negative;
rows with zero weight are dropped.}

\item{bin_scope}{One of \code{"global"} or \code{"node"}. With \code{"global"},
numeric bins are computed once for the full dataset and reused at all nodes.
With \code{"node"}, numeric bins are computed within each node at each depth.}

\item{n_bins}{Integer number of bins for numeric split candidates. Default is 128.}

\item{bin_method}{Binning method for numeric features. One of \code{"quantile"}
(default) or \code{"width"}.}

\item{bin_sample_frac}{Optional fraction in (0,1] used to compute global bins.
If \code{NULL} (default), sampling is automatic: 1\% for datasets exceeding
1 million rows and 100\% otherwise.}

\item{max_depth}{Maximum tree depth (root is depth 0). Default is 5.}

\item{min_split}{Minimum number of rows required to split a node. Default is 20.}

\item{min_leaf}{Minimum number of rows required in each leaf. Default is 5.}

\item{min_gain}{Minimum SSE reduction required to accept a split. Default is 0.}

\item{max_cat}{Maximum distinct categories allowed for categorical predictors.
Default is 50.}

\item{mtry}{Optional integer giving the number of predictors randomly
considered at each split. If \code{NULL} (default), all predictors are
considered.}

\item{seed}{Optional integer random seed used when \code{mtry} is smaller
than the total number of predictors.}

\item{drop_missings}{Logical indicating whether incomplete cases (i.e., rows
where any of the dependent, independent or FE variables are
missing) should be dropped. The default is \code{TRUE}, according with standard
regression software. It is \emph{strongly} recommended not to change this value
unless you are absolutely sure that your data have no missings and you wish
to skip some internal checks. (Even then, it probably isn't worth it.)}

\item{verbose}{Logical. Print progress messages? Defaults to \code{FALSE}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A list of class \code{"dbtree"} containing:
\describe{
\item{nodes}{Data frame describing the fitted tree.}
\item{metrics}{List with training RMSE and R-squared.}
\item{params}{List of tuning parameters.}
}
}
\description{
Fits an approximate regression tree using SQL aggregation on a database backend.
Numeric splits are evaluated on binned candidates (quantiles or equal-width),
while categorical splits are evaluated as one-vs-rest. The heavy lifting
(binning and sufficient statistics) happens in the database; only aggregated
results are returned to R.
}
\examples{
# Simple in-memory example
df = data.frame(y = c(1, 1, 1, 10, 10, 10), x = c(0, 0, 0, 1, 1, 1))
mod = dbtree(y ~ x, data = df, max_depth = 1, n_bins = 2)
predict(mod, df)
}
